# detectors/audio.py
from typing import Optional
def score_audio(path: str) -> float:
    """
    Placeholder MVP: ritorna 0.45 "neutro".
    (Se vuoi, in seguito analizziamo VAD/spettro per coerenza.)
    """
    return 0.45
